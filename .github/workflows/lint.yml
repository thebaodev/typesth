name: 'test'

on:
  push:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    name: 'lint'

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '16'
      - run: npm run lint
